# Copyied for macOS

Run the local macOS launcher from the project root:

```bash
chmod +x launch.command build-mac.sh
./launch.command
```

To create a standalone app:

```bash
./build-mac.sh
open dist/Copyied.app
```

The same build is also available through GitHub Actions on a macOS runner.
